const validateFieldTitle = (request, response, next) => {
  next();
};

const validateFieldStatus = (request, response, next) => {
  next();
};

module.exports = {
  validateFieldTitle,
  validateFieldStatus
}
  